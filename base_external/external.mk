# target = aesd-assignments
# # This is the Makefile for the base external project for AELD assignments
# It includes the necessary configurations and files to support the assignments.
# Maintainer: VasuPadsumbia
include $(sort $(wildcard ../base_external/package/*/*.mk))